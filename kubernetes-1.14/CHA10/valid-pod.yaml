apiVersion: v1
kind: Pod
metadata:
  name: valid-pod
  labels:
    name: valid-pod
spec:
  containers:
  - name: kubenetes-serve-hostname
    image: anjia0532/serve_hostname
    resources:
      limits:
        cpu: "1"
        memory: 512Mi
